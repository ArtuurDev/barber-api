
export interface BarberPorps {
    name: string
    email: string
    phone: string
    Attachments: string[]
    active: boolean
    description?: string
    appointments_id: string[]
    services_id: string[]
}

export class Barber {
    
}